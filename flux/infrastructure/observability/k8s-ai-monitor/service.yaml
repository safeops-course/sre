apiVersion: v1
kind: Service
metadata:
  name: k8s-ai-monitor
  labels:
    app.kubernetes.io/name: k8s-ai-monitor
spec:
  selector:
    app.kubernetes.io/name: k8s-ai-monitor
  ports:
    - name: http
      port: 8080
      targetPort: http
