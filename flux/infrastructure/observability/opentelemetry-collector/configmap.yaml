---
apiVersion: v1
kind: ConfigMap
metadata:
  name: otel-collector-config
  namespace: observability
data:
  config.yaml: |
    receivers:
      # Filelog receiver for collecting logs from Kubernetes pods
      filelog:
        include:
          - /var/log/pods/*/*/*.log
        start_at: beginning
        include_file_path: true
        include_file_name: false
        operators:
          # Parse JSON logs
          - type: json_parser
            id: parser-json
            if: 'body matches "^{\".*}$"'
            timestamp:
              parse_from: attributes.time
              layout: '%Y-%m-%dT%H:%M:%S.%LZ'
          # Extract severity
          - type: severity_parser
            parse_from: attributes.level
            if: 'attributes.level != nil'
          # Add resource attributes
          - type: add
            field: resource.service.name
            value: EXPR(body.service)

    processors:
      # Batch processor groups data before sending (improves performance)
      batch:
        send_batch_size: 1000
        timeout: 10s

      # Memory limiter prevents OOM by dropping data when memory limit is reached
      memory_limiter:
        check_interval: 1s
        limit_mib: 512
        spike_limit_mib: 128

      # Resource processor adds resource attributes
      resource:
        attributes:
          - key: deployment.environment
            value: ${DEPLOYMENT_ENVIRONMENT}
            action: upsert
          - key: service.namespace
            value: ${K8S_NAMESPACE}
            action: upsert

      # Attributes processor for filtering/transforming attributes
      attributes:
        actions:
          - key: uptrace.dsn
            action: delete  # Don't send DSN in telemetry data

    exporters:
      # Uptrace exporter (uses OTLP protocol)
      otlp/uptrace:
        endpoint: api.uptrace.dev:4317
        headers:
          uptrace-dsn: ${UPTRACE_DSN}
        compression: gzip

      # Debug exporter for troubleshooting (logs to stdout)
      logging:
        verbosity: basic
        sampling_initial: 5
        sampling_thereafter: 200

    service:
      pipelines:
        # Logs pipeline — filelog → Uptrace
        logs:
          receivers: [filelog]
          processors: [memory_limiter, batch, resource, attributes]
          exporters: [otlp/uptrace]
