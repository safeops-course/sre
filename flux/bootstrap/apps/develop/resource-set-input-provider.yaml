---
apiVersion: fluxcd.controlplane.io/v1
kind: ResourceSetInputProvider
metadata:
  name: backend-develop
  namespace: flux-system
  annotations:
    fluxcd.controlplane.io/reconcileEvery: "1m"
spec:
  type: OCIArtifactTag
  url: oci://${image_registry}/backend
  filter:
    includeTag: "develop"
    limit: 1
