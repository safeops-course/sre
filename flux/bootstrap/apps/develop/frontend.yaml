---
apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: frontend-develop
  namespace: flux-system
spec:
  interval: 5m0s
  sourceRef:
    kind: GitRepository
    name: flux-system
    namespace: flux-system
  path: ./flux/apps/frontend/overlays/develop
  prune: true
  wait: true
  timeout: 3m
  postBuild:
    substitute:
      NAMESPACE: "develop"
      ENVIRONMENT: "development"
    substituteFrom:
      - kind: ConfigMap
        name: cluster-config
  dependsOn:
    - name: flux-system
    - name: observability
