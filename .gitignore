.DS_Store
.env.local
/bin/
/node_modules/
/.venv/
/dist/
__pycache__/
*.tfstate
*.tfstate.backup
# terraform state rm / apply backups: terraform.tfstate.<timestamp>.backup - they hold secrets
*.tfstate.*
terraform.tfstate.d/
# Saved plans can hold secret values in plain text - never commit them
tfplan
tfplan.meta
tfplan.meta.bak
*.tfplan
infra/terraform/kind_cluster/kubeconfig.yaml
site/content/course/
site/public/

# SOPS private keys (generated locally, never committed)
age.agekey
*.agekey
*.agekey.*.bak
